name: GitHub Actions Demo

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10"]

    steps:
      - uses: actions/checkout@v3
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
      - name: Lint with Ruff
        run: |
          pip install ruff
          ruff --format=github --target-version=py310 .
        continue-on-error: true

      - name: SeleniumCICD
           # You may pin to the exact commit or the version.
            # uses: nakajiss/DockerSeleniumAction@e6ea7ae921226473af620875a8853bad0750a033
        uses: nakajiss/DockerSeleniumAction@v1.3.7
          
        with:
            test-plan-path: .venv/web/Tests/test_main_web.py
            # Optional arguments.
            #args: # optional
          
     
      #- name: Test with pytest
        #run: python .venv/web/Tests/test_main_web.py

    

          
 
